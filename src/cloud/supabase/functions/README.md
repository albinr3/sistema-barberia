# Edge Functions

No Edge Functions are implemented in Fase 2.0.

Planned sensitive operations:

- Availability lookup.
- Appointment creation and cancellation with transactional overlap checks.
- Desktop sync event ingestion.
- Conflict resolution.
- Role changes and administrative actions.

These functions should be added only when the exact input/output contracts are approved.
